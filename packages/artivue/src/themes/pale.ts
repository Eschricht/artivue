import type { Theme } from '../types'

const theme: Theme = {
  accent: {
    background: '#6e80f2',
    text: '#ffffff',
  },
  surface: {
    background: '#292c3d',
    text: '#ffffff',
  },
}

export default theme
