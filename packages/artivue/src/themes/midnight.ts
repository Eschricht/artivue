import type { Theme } from '../types'

const theme: Theme = {
  accent: {
    background: '#9e4cf0',
    text: '#ffffff',
  },
  surface: {
    background: '#161616',
    text: '#ffffff',
  },
}

export default theme
